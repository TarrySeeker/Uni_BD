-- =============================================================================
-- 0041_orders_delivery_city_code — код города СДЭК на заказе
--
-- 🔴 ЭТА МИГРАЦИЯ ПЕРЕНУМЕРОВАНА: на боевом магазине она накатана как 0034.
--
-- ОТКУДА ВЗЯЛАСЬ. Её не было ни в одной ветке кита: функционал жил ТОЛЬКО на
-- проде одного магазина, куда попал деплоем в обход репозитория (`/opt/admik`
-- не git-репо, образы без версионных тегов — установить ревизию кода на проде
-- нечем, поэтому расхождение долго не замечалось).
--
-- ПОЧЕМУ НОМЕР СМЕНИЛСЯ, А НЕ СОХРАНЁН. Номер 0034 в ветке `LK` занят СОВСЕМ
-- ДРУГОЙ миграцией — из личного кабинета покупателя. Один номер означал бы
-- разное содержимое в разных линиях: ровно та коллизия, от которой
-- предостерегает CLAUDE.md и которая это расхождение и породила. Поэтому взят
-- свободный номер ВЫШЕ всех занятых в обеих ветках.
--
-- ПОВТОРНЫЙ НАКАТ НА ТОТ ПРОД БЕЗОПАСЕН: все операции идемпотентны
-- (ADD COLUMN IF NOT EXISTS / CREATE INDEX IF NOT EXISTS / ON CONFLICT
-- DO NOTHING / проверка pg_constraint). Колонки там уже существуют — накат
-- станет no-op и лишь зарегистрирует версию под новым номером. Старая запись
-- 0034 в schema_migrations остаётся как след истории и ничему не мешает:
-- накат идёт по именам файлов, сплошная нумерация не требуется.
-- =============================================================================

-- =============================================================================
-- 0034_orders_delivery_city_code.sql  (боевой аудит СДЭК 2026-07-09, фикс 4)
-- Числовой код города СДЭК получателя на заказе (orders.delivery_city_code).
--
-- Документация apidoc.cdek.ru (POST /v2/orders): to_location для тарифов
-- «до двери» требует address (обязателен) + идентификацию города
-- (code | city | postal_code). Раньше заказ хранил только строковое
-- delivery_city — накладная курьерки уходила без надёжной идентификации города.
-- Витрина уже шлёт delivery.cityCode (из автокомплита /cities); теперь он
-- персистится и используется buildPayload (lib/cdek/services/order.ts).
--
-- NULLable: код города опционален (fallback — строковое delivery_city).
-- Универсально (код города СДЭК, не привязан к конкретному магазину).
--
-- Идемпотентно/аддитивно: ADD COLUMN IF NOT EXISTS. GRANT не нужен: права на
-- orders выданы ранее (полный DML admik_app), новые столбцы наследуют табличные
-- привилегии. Запись в schema_migrations — ON CONFLICT DO NOTHING.
-- =============================================================================

ALTER TABLE orders ADD COLUMN IF NOT EXISTS delivery_city_code integer;

INSERT INTO schema_migrations (version, name)
VALUES ('0041', 'orders_delivery_city_code')
ON CONFLICT DO NOTHING;
