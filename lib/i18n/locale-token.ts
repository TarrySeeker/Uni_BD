/**
 * Клиент-безопасный leaf-модуль i18n: работа с ТЕГОМ языка как со строкой.
 *
 * WHY отдельный файл: lib/i18n/config.ts читает shop_settings и потому тянет
 * репозиторий настроек → lib/db/client → драйвер postgres (node-only fs/net/tls).
 * Клиентским формам нужна только нормализация тега, но импорт из config.ts
 * протаскивал в браузерный бандл всю серверную цепочку и ронял прод-сборку.
 * Здесь — ноль зависимостей, кроме типов; импортировать из клиента безопасно.
 */

import type { Locale } from './types';

/** Нормализует тег языка: обрезка пробелов + нижний регистр ('EN' → 'en'). */
export function normalizeLocale(raw: string): Locale {
  return raw.trim().toLowerCase();
}
