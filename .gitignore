# Dependencies
node_modules/
.pnpm-store/

# Next.js
.next/
out/
# Только КОРНЕВОЙ каталог сборки. Без ведущего слэша паттерн матчит любой вложенный
# `build/` — из-за этого `tests/build/` (guard-тесты границы client/server и паритета
# крон-расписания) молча не попадал в репозиторий: тесты жили только в рабочем дереве.
/build/
dist/

# Env & secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Logs
*.log
npm-debug.log*
logs/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# TypeScript build info
*.tsbuildinfo

# Test / coverage
coverage/
playwright-report/
test-results/

# Docker volumes / data
data/
.data/
postgres-data/
minio-data/

# Бэкапы БД и медиа (Этап 6, 6.2) — содержат данные магазина, не в git
backups/

# Макет витрины конкретного магазина кладётся рядом в каталог storefront/
# (см. START-HERE.md и docs/20). Его внутренние node_modules/.next/.env должны
# игнорироваться вложенным storefront/.gitignore — в этот репозиторий платформы
# исходники витрины не коммитятся.

# Артефакты MCP-playwright (скриншоты/трейсы ручных прогонов)
.playwright-mcp/
