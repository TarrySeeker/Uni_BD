# 20 — Плейбук онбординга: прикрутить админку Admik и БД к готовому макету магазина

> **Это пошаговый ранбук для автономной сессии Claude Code.** Цель — взять
> репозиторий Uni_BD (готовую платформу Admik) и **прикрутить её к уже
> существующему макету витрины** конкретного интернет-магазина, ретематизировав
> данные под нишу магазина и **не меняя бизнес-логику платформы**.
>
> Ранбук идёт в **точном порядке владельца-непрограммиста**: сначала преполёт,
> затем **изучаем готовый фронтенд**, затем **детальное интервью простыми словами**,
> и только потом **стадийно поднимаем БД**, ретематизируем, сращиваем и проверяем.
>
> Прежде чем начать: прочитай `START-HERE.md`, `CLAUDE.md`, `docs/21` (контракт API),
> `docs/22` (опросник интервью). Этот плейбук исполняется скиллом `/onboard-shop`
> (он оркеструет этапы субагентами), но ты можешь идти и вручную по этапам ниже.
>
> **Инварианты (нарушать нельзя):** TDD (сначала тест) · две проверки (код+браузер) ·
> мультитенантность (ноль хардкода) · миграции иммутабельны/аддитивны · секреты
> только из env · документировать каждую правку.

---

## Принцип чекпоинтов и резюмируемости

Владелец — не программист, и сессия может прерваться в любой момент. Поэтому
**каждый Этап завершается чекпоинтом**:

1. **Запись в `docs/00`** (журнал: что сделано на этапе, какие решения приняты) **и
   в `docs/10`** (текущее состояние + «как продолжить ровно с этого места»).
2. **Список «что нужно от владельца, чтобы продолжить»** — недостающие данные, ключи,
   решения, тексты. Отсутствие данных **не повод останавливаться**: помечай пункт,
   подставляй заглушку/mock и двигайся дальше по тому, что можно сделать без владельца.

Возобновление прерванной сессии — всегда с чтения `docs/10` + `docs/00`. Именно
чекпоинты позволяют новой сессии продолжить онбординг без потери контекста.

---

## Обзор этапов (порядок владельца)

| Этап | Что делаем | Главный результат |
|---|---|---|
| **0** | Преполёт: настроить инструменты, поставить зависимости, найти макет, `.env` | Окружение готово |
| **1** | **Изучить фронтенд первым** → «профиль магазина» | Карта данных магазина из макета |
| **2** | **Детальное интервью** с владельцем простыми словами (`docs/22`) | Профиль дополнен |
| **3** | **Стадийно поднять БД** + миграции + seed (`make up → init → smoke`) | Рабочая БД + админка |
| **4** | Ретематизация: каталог/атрибуты + брендинг (`.env` + `shop_settings`) | Магазин «под нишу» |
| **5** | Сращивание: подключить макет к Storefront API | Витрина живёт на Admik |
| **6** | Две проверки (gate + браузер) + документирование | Зелёные проверки, контекст сохранён |

---

## Этап 0 — Преполёт

1. **Настрой инструменты Claude:** `bash .claude/setup.sh` — записывает
   `settings.local.json` (включает MCP-серверы сразу, до trust-диалога) и проверяет
   предпосылки (Node ≥ 20, ключи). Один раз после клонирования.
2. **Поставь зависимости платформы:** `pnpm install`.
3. **Найди макет витрины.** Ожидается, что готовый макет магазина лежит рядом —
   рекомендуемый путь `./storefront` (он уже прописан в `docker-compose.yml`
   как build-context и в `.gitignore`). Если макета нет или он в другом месте:
   - попроси владельца положить его в `./storefront`, **или**
   - уточни путь и поправь `context:` сервиса `storefront` в `docker-compose.yml`.
4. **Заведи `.env`:** `cp .env.example .env`. Пока можно с дефолтами (mock-режим
   интеграций, локальные пароли) — финально заполнишь в Этапе 4.
5. **Прочитай ключевое:** `docs/02` (модель развёртывания), `docs/09` (разворачивание),
   `docs/21` (контракт API), `docs/16` (стек). Для конкретных API библиотек (Next 16,
   React 19, Zod 4, postgres.js) — сверяйся через MCP **context7**, не по памяти.

> **Интервью тут ещё НЕ проводим.** Сначала (Этап 1) изучаем готовый фронтенд, чтобы
> интервью в Этапе 2 шло предметно — по тому, что уже видно в макете.

**Чекпоинт Этапа 0:** инструменты настроены, `pnpm install` прошёл, макет на месте
(`./storefront` или поправлен build-context), `.env` создан. Запиши в `docs/00`/`docs/10`.

**Что нужно от владельца, чтобы продолжить:** путь к готовому макету витрины (если его
ещё нет в `./storefront`).

---

## Этап 1 — Изучить фронтенд первым → «профиль магазина»

Делаем это **до интервью и до БД**. Цель — понять, какие данные нужны магазину, чтобы
интервью шло предметно, а ретематизация и сращивание были спланированы. Запусти
**параллельных read-only** исследователей (агент `Explore`) по макету `./storefront`.
Собери **профиль магазина**:

1. **Каталог:** какие категории/разделы; какие у товара поля (название, цена,
   старая цена, описание, бренд); есть ли **варианты** (размер/цвет/объём) →
   ложатся в `product_variants`; какие **атрибуты/характеристики** нужны (EAV:
   `attributes` + `attribute_values`); единицы (вес/габариты — для расчёта СДЭК).
2. **Навигация и страницы:** меню шапки/футера; статические страницы (О нас,
   доставка, оплата, оферта, контакты) → это CMS-страницы (`cms_pages`).
3. **Брендинг:** название, логотип, favicon, фирменные цвета, контакты, соцсети,
   реквизиты юрлица → `shop_settings.branding/contacts/legalEntity`.
4. **Главная страница:** блоки hero/about/«преимущества»/доставка → `shop_settings.home`.
5. **Корзина/оформление/оплата/доставка:** как сейчас устроено в макете (статика?
   свой бэкенд? localStorage?) — это будет заменено вызовами Storefront API.
6. **Стек витрины:** Next.js App/Pages Router? где статические данные каталога;
   где формы; есть ли свой Dockerfile (`output: standalone`, порт 3000).

**Чекпоинт Этапа 1:** структурированный **черновой профиль магазина** (категории,
атрибуты, варианты, страницы, брендинг, способ оформления/оплаты, стек витрины)
зафиксирован в `docs/00`. Он — вход для Этапов 2, 4 и 5.

**Что нужно от владельца, чтобы продолжить:** ничего (этап read-only). Составь список
**«что не удалось вычитать из макета»** — это станет приоритетными вопросами Этапа 2.

---

## Этап 2 — Детальное интервью с владельцем (простыми словами)

Теперь, опираясь на профиль из Этапа 1, пройди с владельцем **опросник
[`docs/22-интервью-владельца.md`](22-интервью-владельца.md)** — 14 блоков простым
нетех-языком:

1. О магазине и товарах (название, ниша, лого/цвета).
2. Категории (разделы и подразделы, порядок в меню).
3. Варианты товара (размеры / цвета / объёмы; наличие и цена по варианту).
4. Бренды и характеристики (фильтры каталога).
5. Цены, валюта, наличие (старая цена, порог бесплатной доставки).
6. Доставка (нужен ли СДЭК; город отправления; вес/габариты).
7. Оплата (нужен ли Т-Банк; нужен ли чек 54-ФЗ и данные для него).
8. Реквизиты, контакты, юр.данные (для чеков и футера).
9. Домены и адреса сайта (витрина, админка, VPS).
10. Какие модули нужны (`catalog,orders,cdek,cms,payments`).
11. Брендинг: логотип, цвета, слоган.
12. Главная страница: тексты и блоки.
13. Страницы и политики (CMS: оферта, политика конфиденциальности и т.п.).
14. Что запускаем сейчас: показ (mock) или боевой режим.

**Опирайся на профиль из Этапа 1:** что видно в макете — подтверждай, а не спрашивай
с нуля; чего в макете нет (реквизиты, ключи, юр.данные, домены) — спрашивай подробно.
Говори без жаргона; каждый блок `docs/22` подсказывает, **куда попадёт ответ** (env /
`shop_settings` / каталог / CMS) — это для тебя, владельцу читать не обязательно.

**Чекпоинт Этапа 2:** профиль магазина дополнен ответами интервью и записан в `docs/00`.

**Что нужно от владельца, чтобы продолжить:** всё, что владелец пометил «уточнить
позже» (реквизиты/ИНН, боевые ключи СДЭК/Т-Банка, тексты оферты/политики, купленный
домен). Занеси это в список — на mock-показ оно не влияет, понадобится к боевому запуску.

---

## Этап 3 — Стадийно поднять БД + миграции + seed

Поднимаем БД **на своём VPS** (или локально для разработки) **по одному шагу с
проверкой** — это даёт чёткие чекпоинты.

Вариант А (рекомендуется, как на бою) — через Docker:
```bash
make up            # docker compose up -d: app + postgres + redis + minio + caddy
make init          # scripts/init-shop.sh ВНУТРИ контейнера app:
                   #   extensions+роли БД → миграции 0001..NNNN → seed permissions/roles
                   #   → seed владельца (OWNER_EMAIL/PASSWORD) → опц. demo-каталог
make smoke         # health-проверки /api/health и ключевых эндпоинтов
```
Вариант Б (БЕЗ Docker / БЕЗ sudo — напр. автоматизированная сессия ассистента на
«голом» хосте) — полностью в **docs/25-без-docker-профиль.md**. Кратко:
```bash
pnpm install                        # ставит embedded-postgres (npm) → Postgres без Docker/sudo
node scripts/dev-db/start.mjs &     # локальный Postgres на 127.0.0.1:5442
PGHOST=127.0.0.1 PGPORT=5442 PGUSER=postgres PGPASSWORD=postgres \
  node scripts/dev-db/init-db.mjs   # схема+роли+seed через postgres.js (без psql, обходит 0001)
# Redis/MinIO НЕ нужны: оставь REDIS_URL/S3_* пустыми → in-memory rate-limit + локальное
# хранилище, /api/health помечает их 'skipped' (критичен только 'db').
export DATABASE_URL=postgres://admik_app:app-local@127.0.0.1:5442/admik
pnpm dev
```
Боевой деплой без Docker — опциональный bare-metal профиль `scripts/bare-metal/` (Caddy + systemd), тоже в docs/25.

Что важно знать про `init-shop.sh` (идемпотентен, см. `docs/09`/`docs/12`):
- роли БД least-privilege: `admik_migrator` (DDL/владелец таблиц), `admik_app`
  (рантайм DML; на `audit_log` только SELECT/INSERT — append-only);
- миграции накатываются по порядку имён, журнал в `schema_migrations`;
- сид владельца: при пустом `OWNER_PASSWORD` пароль генерируется и печатается ОДИН раз;
- демо-каталог — только при `SEED_DEMO_CATALOG=true` (для smoke; на бою `false`).

**Проверь:** `GET /api/health` → `{"status":"ok"}` (и `?deep=1` — с проверкой БД/Redis/S3);
вход в `/admin/login` под `OWNER_EMAIL`; в админке открываются разделы включённых модулей.

**Чекпоинт Этапа 3:** БД поднята, все миграции накатаны, `health` зелёный, владелец
заходит в админку. В `docs/00`/`docs/10` зафиксируй, **как переподнять стек** (команды
выше) и сгенерированный пароль владельца (если генерировался — только факт генерации, не
сам пароль в git).

**Что нужно от владельца, чтобы продолжить:** если это уже боевой VPS — надёжные пароли
БД/MinIO/`OWNER_PASSWORD`/`ORDER_TOKEN_SECRET` и домены (иначе поднимаемся на дефолтах/
локально и вернёмся к этому на боевом деплое, `docs/09`).

---

## Этап 4 — Ретематизация под нишу магазина (БЕЗ правок бизнес-логики)

Перенос на новый магазин = **конфигурация + данные**, не код (ADR-003; скилл `new-shop`).
Источник данных — профиль магазина (Этапы 1+2).

### 4.1 `.env` под магазин
Заполни (см. `.env.example` и `START-HERE.md §6`): `SHOP_NAME`, `SHOP_DOMAIN`,
`ADMIN_DOMAIN`, `SHOP_ORDER_PREFIX`, `SHOP_CURRENCY`, `ADMIK_MODULES` (только нужные
модули), `STOREFRONT_ALLOWED_ORIGINS`/`STOREFRONT_API_KEYS`,
`NEXT_PUBLIC_ADMIK_API_URL`, доступы БД/MinIO/Redis. `CDEK_*`/`TBANK_*` — оставь
пустыми для mock, боевые ключи подставишь только на сервере.

### 4.2 Каталог под нишу
Два пути (выбери по ситуации):
- **Через админку (предпочтительно для боевого наполнения):** владелец заводит
  категории/атрибуты/бренды/товары в `/admin/catalog/*`. Твоя задача — убедиться,
  что структура (категории, варианты, атрибуты) из «профиля магазина» поддержана.
- **Через сид (для демо/smoke/быстрого старта):** адаптируй `db/seed/demo-catalog.sql`
  под нишу — это нейтральный идемпотентный пример (категории, атрибуты Цвет/Размер,
  товары, варианты, остатки на естественных ключах `slug/sku/code`). Замени
  нейтральные значения на категории/атрибуты/товары магазина. **TDD:** обнови
  соответствующий seed-тест в `tests/seed/` под новые данные, прогони `pnpm test`.
  Накат — под флагом `SEED_DEMO_CATALOG` через `init-shop.sh`.

> Структура каталога: EAV-атрибуты (`attributes`/`attribute_values`/
> `product_attributes`, `is_variant` для вариантных), варианты `product_variants`
> (цена `price_override`/`price_delta`, остатки в `inventory`), медиа `product_media`.
> Вес/габариты товара/варианта — для расчёта СДЭК (миграции 0018/0026).

### 4.3 Брендинг и контент через `shop_settings`
Через админку `/admin/settings` (разделы: Брендинг, Главная, Навигация и футер,
Валюта и единицы, Реквизиты и контакты, Каталог/доставка/заказы, Модули, SEO) задай
данные магазина из профиля. Они сразу доезжают до витрины через `GET /settings`.
Кэш эффективных настроек инвалидируется автоматически при правке через UI; при
прямой правке БД нужен `docker compose restart app`.

### 4.4 CMS-страницы
Статические страницы магазина (О нас, доставка, оферта) заведи в `/admin/cms` —
витрина читает их через `GET /pages` / `GET /pages/:slug`.

**Чекпоинт Этапа 4:** `.env` заполнен под магазин; структура каталога/атрибутов
соответствует нише; брендинг/контакты/навигация/главная заданы; CMS-страницы есть;
демо-сид (если используешь) ретематизирован и seed-тесты зелёные. В `docs/00` —
**список изменённых конфиг/сид-файлов**.

**Что нужно от владельца, чтобы продолжить:** боевое наполнение каталога и финальные
тексты страниц (оферта/политика) — если их пока нет, идём на демо/черновиках и метим
пункт в список.

---

## Этап 5 — Сращивание: подключить макет витрины к Storefront API

Переводим макет из «статики/своего бэкенда» в **чистого потребителя** API.
Полный контракт — `docs/21`. Метод и worked-example — `docs/13`.

### 5.1 Конфигурация витрины (Next.js)
- **Два адреса API намеренно:**
  - SSR (server components / route handlers витрины) → `http://app:3000` (внутри
    docker-сети), переменная `ADMIK_API_URL`;
  - браузерный бандл (checkout, автокомплит города) → `https://${ADMIN_DOMAIN}`,
    переменная `NEXT_PUBLIC_ADMIK_API_URL` (build-arg, «впекается» в сборку).
- **Авторизация:** для SSR — заголовок `X-Storefront-Key: <ключ>` (значение из
  `STOREFRONT_API_KEY`, и оно же в `STOREFRONT_API_KEYS` на стороне Admik); для
  браузера достаточно совпадения `Origin` ∈ `STOREFRONT_ALLOWED_ORIGINS`. Локально
  можно оставить mock-режим (ключи/origins пусты) — открыто всем.
- **Медиа:** изображения приходят готовыми публичными URL (через Caddy `/media/*`
  → MinIO). Дополнительная проксировка не нужна.

### 5.2 Замена данных макета на вызовы API (по маппингу `docs/21 §7`)
Пройди по макету и замени источники данных:
- статический список товаров → `GET /products` (+фильтры);
- карточка → `GET /products/:slug`; навигация → `GET /categories`/`GET /brands`;
- корзина-предпросмотр → `POST /cart/quote` (источник истины по цене/итогу);
- оформление → `POST /orders` (сохрани `accessToken` для ЛК/оплаты);
- трекинг/ЛК → `GET /orders/:number?token=`;
- доставка СДЭК → `cities`/`pvz`/`calculate`; оплата → `POST /payments/tbank/init`
  и редирект на `paymentUrl`;
- статические страницы → `GET /pages*`; шапка/футер/логотип/цвета/контакты/главная
  → `GET /settings`; промо-баннер → `GET /promotions`; формы → `POST /leads`/`/newsletter`.

**Anti-tamper:** НЕ отправляй цены/итоги/вес с витрины — сервер их игнорирует и
считает сам. Витрина передаёт только выбор (id/qty/промокод/адрес/способ).

### 5.3 Если в макете чего-то не хватает в API
Сверься с `docs/21`. Если действительно нет нужного публичного эндпоинта — это
расширение платформы: проектируй через агента `solution-architect`, реализуй по
TDD в `app/api/storefront/v1/*` + `lib/storefront/*` (сначала тест), аддитивно.
Не добавляй в витрину обходные пути со своей бизнес-логикой/своей БД.

**Чекпоинт Этапа 5:** витрина рендерит каталог/карточку/категории из API; корзина
считается через `quote`; оформляется заказ; работает доставка и оплата (mock);
брендинг/контент тянутся из `/settings` и `/pages`. В `docs/00` — **статус сращивания
по разделам** (что подключено, что осталось).

**Что нужно от владельца, чтобы продолжить:** обычно ничего (mock-путь проходится без
владельца); для боевого — только ключи из списка Этапа 2.

---

## Этап 6 — Две обязательные проверки + документирование

### 6.1 Две проверки
1. **Код-гейт** — скилл `/gate`: `pnpm typecheck` (0) · `pnpm lint` (0) ·
   `pnpm test` (vitest, все зелёные) · `scripts/check-migrations.sh` (аддитивность) ·
   `security-review` при правках оплаты/RBAC/токенов/webhook/секретов. ВСЕГДА перед коммитом.
2. **Живой прогон в браузере** — скилл `/deploy-stand` (на стенде) или `/verify` +
   MCP **playwright** локально: пройди путь покупателя (каталог → карточка → корзина
   → оформление → доставка СДЭК → оплата mock) и админку (логин → завести товар →
   увидеть его на витрине). Только mock-режим интеграций.
3. **Интеграции** — скилл `/integration-check`: e2e СДЭК + Т-Банк в mock end-to-end.
4. **Smoke** — `make smoke` / `scripts/smoke.sh`: health + ключевые эндпоинты.

### 6.2 Документирование и коммит
- Обнови `docs/00` (журнал: что сделано, решения), `docs/10` (состояние/как продолжить),
  README инстанса (как поднять именно этот магазин).
- Зафиксируй в `docs/00` итоговый профиль магазина и список изменённых конфиг/сид-файлов.
- Коммить только после ОБЕИХ проверок. Сообщения — осмысленные, по-русски.
- Боевой деплой на VPS — по `docs/09` (`server-bootstrap.sh` → `.env` с боевыми
  секретами → `make deploy`); на бою `SEED_DEMO_CATALOG=false`, реальные ключи
  СДЭК/Т-Банк только при включении модулей.

**Финальный чекпоинт Этапа 6:** обе проверки зелёные (код + браузер), integration-check
и smoke прошли, документация обновлена — **Definition of Done достигнут**.

**Что нужно от владельца, чтобы продолжить (на боевой запуск):** боевые ключи СДЭК/Т-Банка,
реальные пароли и купленный домен (см. накопленный список из Этапов 2–4).

---

## Чеклист приёмки онбординга

- [ ] Инструменты настроены (`.claude/setup.sh`), макет на месте (`./storefront`), `pnpm install` ок.
- [ ] Профиль магазина собран (Этап 1 из макета + Этап 2 из интервью `docs/22`).
- [ ] БД поднята, все миграции накатаны, `/api/health` = ok, владелец заходит в `/admin`.
- [ ] `.env` заполнен под магазин; ноль хардкода ниши в коде.
- [ ] Каталог/атрибуты/варианты соответствуют нише; seed-тесты зелёные (если сид).
- [ ] Брендинг/контакты/навигация/главная/CMS заданы и доезжают до витрины.
- [ ] Витрина потребляет Storefront API по всем нужным разделам (см. `docs/21 §7`).
- [ ] `/gate` зелёный; живой прогон в браузере пройден; `/integration-check` ок; smoke ок.
- [ ] Каждый этап закрыт чекпоинтом; `docs/00`, `docs/10`, README инстанса обновлены.

---

## Troubleshooting

| Симптом | Причина / решение |
|---|---|
| `401` от Storefront API | Нет ключа/Origin. Задай `STOREFRONT_API_KEYS` + `X-Storefront-Key` (SSR) или `STOREFRONT_ALLOWED_ORIGINS` (браузер); локально — оставь оба пустыми (mock). |
| Раздел админки/эндпоинт `404`/`503` | Модуль выключен. Включи в `ADMIK_MODULES` (рестарт) или `shop_settings.module_overrides` (скилл `/module-toggle`). |
| Правка настройки не видна на витрине | Прямая правка БД не инвалидирует кэш → `docker compose restart app`. Через UI — инвалидируется автоматически. |
| `check-migrations.sh` падает | Ты отредактировал/добавил деструктивную миграцию. Миграции иммутабельны и только аддитивны (без DROP/RENAME/TYPE/SET NOT NULL без DEFAULT). |
| Цена/итог на витрине не совпадает | Витрина шлёт свои суммы — сервер их игнорирует. Источник истины — `quote`/`orders`. Убери клиентский расчёт. |
| СДЭК/оплата «не настоящие» локально | Это mock (пустые `CDEK_*`/`TBANK_*`) — так и задумано. Demo-оплата — `app/mock/tbank/pay`. Боевые ключи только на сервере. |
| `next build` OOM на слабом VPS | Включи swap (`server-bootstrap.sh`), см. `docs/09`/`docs/12`. |

> Возобновление прерванной сессии: `docs/10` + `docs/00` (последний чекпоинт). Карта
> инструментов (MCP/скиллы/агенты) — `docs/19` и низ `CLAUDE.md`. Опросник интервью — `docs/22`.
