-- =============================================================================
-- 0042_shop_settings_size_charts_seed — сид настройки размерных сеток
--
-- 🔴 ЭТА МИГРАЦИЯ ПЕРЕНУМЕРОВАНА: на боевом магазине она накатана как 0035.
--
-- ОТКУДА ВЗЯЛАСЬ. Её не было ни в одной ветке кита: функционал жил ТОЛЬКО на
-- проде одного магазина, куда попал деплоем в обход репозитория (`/opt/admik`
-- не git-репо, образы без версионных тегов — установить ревизию кода на проде
-- нечем, поэтому расхождение долго не замечалось).
--
-- ПОЧЕМУ НОМЕР СМЕНИЛСЯ, А НЕ СОХРАНЁН. Номер 0035 в ветке `LK` занят СОВСЕМ
-- ДРУГОЙ миграцией — из личного кабинета покупателя. Один номер означал бы
-- разное содержимое в разных линиях: ровно та коллизия, от которой
-- предостерегает CLAUDE.md и которая это расхождение и породила. Поэтому взят
-- свободный номер ВЫШЕ всех занятых в обеих ветках.
--
-- ПОВТОРНЫЙ НАКАТ НА ТОТ ПРОД БЕЗОПАСЕН: все операции идемпотентны
-- (ADD COLUMN IF NOT EXISTS / CREATE INDEX IF NOT EXISTS / ON CONFLICT
-- DO NOTHING / проверка pg_constraint). Колонки там уже существуют — накат
-- станет no-op и лишь зарегистрирует версию под новым номером. Старая запись
-- 0035 в schema_migrations остаётся как след истории и ничему не мешает:
-- накат идёт по именам файлов, сплошная нумерация не требуется.
-- =============================================================================

-- =============================================================================
-- 0035_shop_settings_size_charts_seed.sql
-- Идемпотентный seed ключа настроек 'size_charts' (docs/11 §5.4.2).
--
-- Полностью по образцу 0020_shop_settings_seed.sql: сидируем раздел ПУСТЫМ
-- объектом '{}'::jsonb. Пустой объект = «нет оверрайда» → при чтении
-- (mergeSettings) раздел падает на платформенный дефолт { charts: [] }, то есть
-- блок размерной сетки на витрине просто не рисуется. Администратор получает
-- готовую строку для правки через /admin/settings/size-charts без ручного INSERT.
--
-- МУЛЬТИТЕНАНТНОСТЬ (docs/02, ADR-003). В миграции НЕТ и не может быть значений
-- конкретного магазина: набор сеток, колонок и цифр у каждого ИМ свой. Реальные
-- размерные сетки магазина «THE CASE» вынесены в db/seed/the-case-size-charts.sql
-- и накатываются вручную на конкретный инстанс (контент магазина ≠ схема).
--
-- ON CONFLICT (setting_key) DO NOTHING (НЕ DO UPDATE): повторный накат миграций
-- НЕ затирает уже отредактированное администратором значение.
-- =============================================================================

INSERT INTO shop_settings (setting_key, value)
VALUES
  ('size_charts', '{}'::jsonb)
ON CONFLICT (setting_key) DO NOTHING;

INSERT INTO schema_migrations (version, name)
VALUES ('0042', 'shop_settings_size_charts_seed')
ON CONFLICT DO NOTHING;
